# frozen_string_literal: true
<% modules = module_name.split('::') %>
<%= modules.each_with_index.map { |part, index| "#{'  ' * index}module #{part}" }.join("\n") %>
<%= modules.each_with_index.map { |_, index| "#{'  ' * (modules.size - index - 1)}end" }.join("\n") %>
